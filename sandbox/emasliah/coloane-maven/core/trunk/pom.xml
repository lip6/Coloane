<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  
  <parent>
    <groupId>fr.lip6.move.coloane</groupId>
    <artifactId>coloane-maven</artifactId>
    <version>1.0-SNAPSHOT</version>
    <!--<relativePath>../../pom.xml</relativePath>-->
  </parent>

	<groupId>${project.parent.groupId}</groupId>
  <artifactId>core</artifactId>
  <version>0.9.2-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>Core - Maven-Packaging</name>
  <description>Core</description>
  
  <scm>
  	<connection>scm:svn:https://srcdev.lip6.fr/svn/research/jbvoron/projets/coloane/sandbox/emasliah/${project.parent.artifactId}/${project.artifactId}/trunk</connection>
   	<developerConnection>scm:svn:https://srcdev.lip6.fr/svn/research/jbvoron/projets/coloane/sandbox/emasliah/${project.parent.artifactId}/${project.artifactId}/trunk</developerConnection>
 		<url>https://srcdev.lip6.fr/trac/research/jbvoron/browser/projets/coloane/sandbox/emasliah/${project.parent.artifactId}/${project.artifactId}/trunk</url>
  </scm>
  
	<dependencies>
    <dependency>
      <groupId>${project.parent.groupId}</groupId>
      <artifactId>fr.lip6.move.coloane.interfaces</artifactId>
      <version>1.0.3-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>${project.parent.groupId}</groupId>
      <artifactId>fr.lip6.move.coloane.apicami</artifactId>
      <version>0.9.8-SNAPSHOT</version>
    </dependency>
  </dependencies>
  
	<build>

		<!-- ~~~~~~~~~~~~~~~~~~~~ Configuration des plugins ~~~~~~~~~~~~~~~~~~~~ -->
			
		<plugins>
		
			<plugin>
				<!-- Pour compiler avec le -classpath -->
    		<groupId>org.apache.maven.plugins</groupId>
      	<artifactId>maven-compiler-plugin</artifactId>
      	<configuration>
       		<compilerArgument>@javac_options</compilerArgument>
      	</configuration>
    	</plugin>
	
	<plugin>
		<!-- Pour supprimer le fichier javac_options -->
		<groupId>org.apache.maven.plugins</groupId>
		<artifactId>maven-clean-plugin</artifactId>
		<configuration>
			<filesets>
				<fileset>
					<directory>.</directory>
					<includes>
						<include>javac_options</include>
					</includes>
				</fileset>
			</filesets>
		</configuration>
	</plugin>
    	
    	<!-- ~~~~~~~~~~~~~~~~~~~~~ Executions des plugins ~~~~~~~~~~~~~~~~~~~~~~
    			 ~~~~~~~~~~~~~~~~~~~~~ S'effectue dans l'ordre ~~~~~~~~~~~~~~~~~~~~~ -->
    	
    	<!-- Fait pas Continuum --> 
	  	<!--<plugin>
    		<!~~ Pour le svn ~~>
       	<groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-scm-plugin</artifactId>
        <executions>  
        	<execution>
        		<!~~ On update ~~>
        		<id>Up</id>
          	<phase>validate</phase>        
          	<goals>
            	<goal>update</goal>
          	</goals>
        	</execution>
        </executions>
      </plugin>-->
      
      <plugin>
      	 <!-- Pour mettre le buildnumber au bout du jar ${buildNumber} 
      	 			/!\ Ne marche que avec svn en anglais -->
      	<groupId>org.codehaus.mojo</groupId>
      	<artifactId>maven-buildnumber-plugin</artifactId>
      	<version>0.9.4</version>
      	<executions>
        	<execution>
        		<id>BuildNumber</id>
          	<phase>validate</phase>        
          	<goals>
            	<goal>create</goal>
          	</goals>
        	</execution>
      	</executions>
     	</plugin>
      
      <!--<plugin>
    		<!~~ Pour modifier le manifest avec la version du pom ~~>
    	  <groupId>org.apache.maven.plugins</groupId>
      	<artifactId>maven-psteclipse-plugin</artifactId>
        <executions>
        	<execution>
          	<phase>validate</phase>        
          	<goals>
            	<goal>eclipse-plugin</goal>
          	</goals>
        	</execution>
        </executions>
    	</plugin>-->
    	
    	<plugin>
    		<!-- Pour creer le fichier javac_options -->  
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>Javac_options</id>
          	<phase>validate</phase> 
            <goals>
              <goal>exec</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <executable>lib.sh</executable>
          <arguments>
            <argument>lib</argument>
          </arguments>
        </configuration>
      </plugin>
      
      <!--<plugin>
    		<!~~ Pour le svn ~~>
       	<groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-scm-plugin</artifactId>
        <executions>  
        	<execution>
        	  <!~~ On rajoute le jar cree ~~>
        		<id>Add</id>
          	<phase>package</phase>        
          	<goals>
            	<goal>add</goal>
          	</goals>
          	<configuration>
          		<includes>target/jar/**</includes>
          	</configuration>
        	</execution>
        	<execution>
        		<!~~ On commit pour mettre le svn a jour ~~>
        		<id>Co</id>
          	<phase>package</phase>        
          	<goals>
            	<goal>checkin</goal>
          	</goals>
          	<configuration>
          		<message>Mise a jour du svn apres package</message>
          	</configuration>
        	</execution>
      	</executions>
      </plugin>-->
      
      <!--<plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-release-plugin</artifactId>
        <configuration>
          <tagBase>https://srcdev.lip6.fr/svn/research/jbvoron/projets/coloane/fr.lip6.move.coloane.interfaces/branches/maven-packaging/target/jar</tagBase>
        </configuration>
      </plugin>-->
      
		</plugins>
		
		<!-- ~~~~~~~~~~~~~~~~~~~~ Ce qu'on va rajouter dans le jar ~~~~~~~~~~~~~~~~~~~~~~~~ -->
		
		<resources>
     <resource>
       <directory>.</directory>
       <includes>
         <include>icons/**</include>
         <include>resources/**</include>
         <include>build.properties</include>
         <include>plugin.properties</include>
         <include>plugin.xml</include>
       </includes>
       <!--<excludes>
         <exclude>bin/**</exclude>
         <exclude>src/**</exclude>
         <exclude>META-INF/**</exclude>
         <exclude>pom.xml</exclude>
         <exclude>target/**</exclude>
       </excludes>-->
     </resource>
   </resources>
  
	</build>
	
</project>
