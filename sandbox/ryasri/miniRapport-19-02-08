Bonjour

N'ayant toujours pas trouvé une solution quand au problème du parser ppour un fichier XML.
j'ai sauté a autre chose, qui est la creation de model COLOANE, et ainsi avec leurs codes sources, essayer d'où peut venir le problème.
Lors de la creationd e modèle COLOANE : pour les : "AMI-NET" pour l'instant aucune constation , aucun problème et leurs fichiers xml resperctifs sont correctes.

Cependant pour les "BRANCHING-PROCESS ", une information manque dans le fichier xml apporprié, c'est la notation du schema "xsd", ce qui pourrait etre aa l'origine des problèmes, voici un exemple:

	-ceci correspond au debut d'un modèle Petri :
		        	  <model xsi:noNamespaceSchemaLocation="petriXschemas.xsd" formalism="AMI-NET" xposition="20" yposition="20">.

	-et ceci celui d'un modèle prefix :
			          <model xsi:noNamespaceSchemaLocation=""                  formalism="BRANCHING-PROCESS" xposition="20" yposition="20">

Notes de par le vide qu'il y'a , l'abscence du fichier XSCHEMA.

Pour cela, quelques fautes de frapes, ainsi que quelques erreurs ont eté corrigées dans le fichier d'origine "XmlEditor.java ".
malgré cette correction, sauf que ce n'est toujours pas bon, pour cela,une revisualisation du fichier XSCHEMA s'oblige.

Après cela, un essai de validation du fichier pas la commande: "xmllint -schema  nom_fichier.xsd nom_fichier.xml", nous prouve que le fichier n'est pas VALIDE.

Par la suite, mes efforts seront sur cette lancée, afin de corriger cela. en traitant en priorité : " XmlEditor.java " & " *.xsd "

Merci a tous, et Bon courage a tous.

